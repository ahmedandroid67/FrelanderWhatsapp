{
  "name": "clientflow-admin",
  "version": "1.0.0",
  "description": "Admin script for generating activation codes",
  "main": "generate-codes.js",
  "scripts": {
    "generate": "node generate-codes.js"
  },
  "dependencies": {
    "firebase-admin": "^12.0.0"
  }
}